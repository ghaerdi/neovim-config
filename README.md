## Configuration for neovim

Move or copy the config files to **$HOME/.config/nvim.** <br>
Opening neovim should autoinstall vim-plug, plugins and coc extensions (maybe need to open neovim 2 times more).